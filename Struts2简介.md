Struts2对比1有哪些新的特性：
	1.用来从动作逻辑中分层出横切关注点的拦截器
	2.为了减少或者消除XML配置基于注解的配置方式
	3.一个贯穿整个框架的强大的表达式语言OGNL(Object-Graph Navigation Language,对象图导航语言)
	4.支持可变更和可重用UI组建的基于微型MVC模式的标签API
	
Struts2如何实现MVC
MVC模式提供的关注点分离非常适用于Web应用程序。
MVC设计模式有3个关注点：模型、视图和控制器。在Struts2中它们分别通过动作(action)、结果(result)和过滤分配器(FilterDispatcher)实现

控制器 FilterDispatcher
Struts2使用的MVC变体经常被成为诶前端控制器MVC，即控制器在最前端。
控制器的角色由Structs2的FilterDispatcher来扮演，他是个Servlet过滤器，检查每个传入请求，决定哪个Struts2动作处理这个请求。
只需要告诉框架哪个请求URL需要映射到哪个动作即可。

模型 动作
从某种程度来讲，模型像是一个包含应用程序内容的黑盒子，专业点说模型是应用程序的内部状态，这个状态由数据模型和业务逻辑共同组成。
动作的作用1，动作将业务逻辑调用封装到一个单独的工作单元中。
2，动作是一个数据传输的场所。

视图 结果
视图是MVC的呈现组件。
视图的作用：将应用程序的状态转换为一种用户可以与之交互的可视化的表示。

良好的MVC模式为轻松地管理最复杂的前端奠定了基础。